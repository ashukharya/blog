<%- include("partials/header") %> 

    <h2 class="hed">Home</h2>

    <p><%= home %> </p>
    <% for(var i=0;i<posts.length;i++){ %>
      <div class="days">
      <h2>
        <%= posts[i].title  %>
      </h2>
      <p>
        <%= posts[i].content.substring(0,100) + "..." %> <a href="/posts/<%=posts[i]._id %>">Read More</a>
      </p>
    </div>
      <% } %>  
      <form action="/" method="post">
        <button class="btn btn-warning" type="submit" >Delete Last Blog</button>
      </form>
    <%- include("partials/footer") %> 